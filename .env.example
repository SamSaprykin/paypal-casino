# Sanity — runtime build & GraphQL codegen (`pnpm sanity:graphql:codegen`)
SANITY_PROJECT_ID=
SANITY_DATASET=production
SANITY_GRAPHQL_TAG=default
SANITY_GRAPHQL_USE_CDN=true

# Optional: use when introspection or dataset reads require auth
SANITY_API_TOKEN=
